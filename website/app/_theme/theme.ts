import { extendTheme } from '@mui/joy';

export const theme = extendTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          mainChannel: '0 90 156',
          plainColor: '#005A9C',
        },
      },
    },
  },
});
